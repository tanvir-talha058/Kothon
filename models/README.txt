Place a Vosk model folder in this directory.

Expected examples:
- models/vosk-model-small
- models/vosk-model-small-en-us-0.15

The app now auto-detects the first folder whose name starts with "vosk-model".
